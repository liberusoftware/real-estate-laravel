<div class="space-y-4"><input wire:model.live="search" type="search" placeholder="Search property management" class="w-full rounded border px-3 py-2"><div class="divide-y rounded border bg-white">@forelse($records as $record)<div class="flex items-center justify-between p-4"><span>{{ $record->subject }}</span><span class="text-sm text-gray-500">{{ str($record->capability)->replace('_',' ')->title() }} · {{ str($record->status->value)->replace('_',' ')->title() }}</span></div>@empty<div class="p-4 text-gray-500">No records found.</div>@endforelse</div>{{ $records->links() }}</div>
